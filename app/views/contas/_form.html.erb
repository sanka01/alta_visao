<%= form_with(model: conta, local: true) do |form| %>
  <% if conta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conta.errors.count, "error") %> prohibited this conta from being saved:</h2>

      <ul>
      <% conta.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :nivel_permissao %>
    <%= form.text_field :nivel_permissao %>
  </div>

  <div class="field">
    <%= form.label :usuario_id %>
    <%= form.text_field :usuario_id %>
  </div>

  <div class="field">
    <%= form.label :oficina_id %>
    <%= form.text_field :oficina_id %>
  </div>

  <div class="field">
    <%= form.label :franquia_id %>
    <%= form.text_field :franquia_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
